steps:
- id: plot rados benchmarks
  uses: docker://czentye/matplotlib-minimal:3.1.2
  runs: ['python', './plot/plot_rados_benchmarks.py']

- id: plot osd benchmarks
  uses: docker://czentye/matplotlib-minimal:3.1.2
  runs: ['python', './plot/plot_osd_benchmarks.py']
